from setuptools import setup

setup(name='pyomega',
      version='0.1',
      description='Transfinite ordinals for Python',
      author='Alex Riley',
      license='MIT',
      packages=['pyomega'],
      zip_safe=False)
